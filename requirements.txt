# Core LangChain / LangGraph
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langgraph>=0.2.0

# LLM providers (choose one via .env)
langchain-anthropic>=0.3.0
langchain-openai>=0.3.0
langchain-groq>=0.3.0

# Embeddings — HuggingFace (local, default, no API key)
langchain-huggingface>=0.1.0
sentence-transformers>=2.7.0
# Embeddings — OpenAI (already included via langchain-openai above)
# Embeddings — Cohere (optional; install if EMBEDDING_PROVIDER=cohere)
langchain-cohere>=0.3.0
# Embeddings — Google Generative AI (optional; install if EMBEDDING_PROVIDER=google)
langchain-google-genai>=2.0.0

# Vector store
chromadb>=0.5.0
langchain-chroma>=0.2.0

# Document format parsers
pypdf>=3.0.0
python-docx>=1.0.0

# FastAPI & server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# Pydantic settings
pydantic>=2.9.0
pydantic-settings>=2.6.0

# HTTP & web scraping
httpx>=0.27.0
requests>=2.32.0
beautifulsoup4>=4.12.0
markdownify>=0.13.0

# Web search fallback (bonus)
tavily-python>=0.3.0

# Streamlit frontend (bonus)
streamlit>=1.38.0

# Utilities
python-dotenv>=1.0.0
aiofiles>=24.1.0
